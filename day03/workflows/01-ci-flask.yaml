name: "04 Flask CI Workflow"
on:
 push:
jobs:
 flask-ci-job:
  runs-on: ubuntu-latest
  steps:
   - name: Checkout Repository
     uses: actions/checkout@v6

   - name: Build Docker image
     run: docker build -t cwj-flask-app:v1.0.0 ./day03/workflows

   - name: Run Container
     run: docker run -d -p 5000:5000 --name flask-container cwj-flask-app:v1.0.0

   - name: Smoke Test
     run: |
      sleep 5
      curl http://localhost:5000/health 